<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.seu.mp.tbl.mapper.UserMapper">

    <select id="selectPageVo" resultType="cn.edu.seu.mp.tbl.entity.User">
        SELECT name FROM tbl_user WHERE state=#{state}
    </select>

</mapper>
